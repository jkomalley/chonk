[project]
name = "chonk"
version = "0.1.0"
description = "Directory size viewer."
readme = "README.md"
authors = [{ name = "Kyle O'Malley", email = "j.kyle.omalley@gmail.com" }]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
chonk = "chonk.cli:main"

[build-system]
requires = ["uv_build>=0.11.16,<0.12.0"]
build-backend = "uv_build"
